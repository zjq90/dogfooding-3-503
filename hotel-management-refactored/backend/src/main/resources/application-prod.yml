server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jiudian?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=Asia/Shanghai
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:123456}

logging:
  level:
    com.hotel: info
  file:
    name: logs/hotel-management.log
