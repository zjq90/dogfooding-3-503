<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hotel.mapper.OrderMapper">

    <select id="selectByRoomId" resultType="com.hotel.entity.Order">
        SELECT room_id, user_name, ID as id, start_time, end_time, prepay, payment, `change`, ser_num
        FROM t_order
        WHERE room_id = #{roomId}
    </select>

    <update id="updateByRoomId" parameterType="com.hotel.entity.Order">
        UPDATE t_order
        SET end_time = #{endTime}, payment = #{payment}
        WHERE room_id = #{roomId}
    </update>

    <delete id="deleteByRoomId">
        DELETE FROM t_order WHERE room_id = #{roomId}
    </delete>
</mapper>
