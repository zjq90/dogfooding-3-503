<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hotel.mapper.UserMapper">

    <select id="selectByNameAndPassword" resultType="com.hotel.entity.User">
        SELECT user_id, user_name, user_pwd, address, phone, email, QQ as qq, photo_url
        FROM t_user
        WHERE user_name = #{userName} AND user_pwd = #{userPwd}
    </select>

    <select id="countByUserName" resultType="int">
        SELECT COUNT(*) FROM t_user WHERE user_name = #{userName}
    </select>
</mapper>
